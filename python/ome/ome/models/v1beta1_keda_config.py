# coding: utf-8

"""
OME

Python SDK for OME

The version of the OpenAPI document: v0.1
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing_extensions import Self


class V1beta1KedaConfig(BaseModel):
    """
    KedaConfig stores the configuration settings for KEDA autoscaling within the InferenceService. It includes fields like the Prometheus server address, custom query, scaling threshold, and operator.
    """  # noqa: E501

    custom_prom_query: Optional[StrictStr] = Field(
        default=None,
        description='CustomPromQuery defines a custom Prometheus query that KEDA will execute to evaluate the desired metric for scaling. This query should return a single numerical value that represents the metric to be monitored.  Example:   avg_over_time(http_requests_total{service="llama"}[5m])',
        alias="customPromQuery",
    )
    enable_keda: Optional[StrictBool] = Field(
        default=None,
        description="EnableKeda determines whether KEDA autoscaling is enabled for the InferenceService. - true: KEDA will manage the autoscaling based on the provided configuration. - false: KEDA will not be used, and autoscaling will rely on other mechanisms (e.g., HPA).",
        alias="enableKeda",
    )
    prom_server_address: Optional[StrictStr] = Field(
        default=None,
        description="PromServerAddress specifies the address of the Prometheus server that KEDA will query to retrieve metrics for autoscaling decisions. This should be a fully qualified URL, including the protocol and port number.  Example:   http://prometheus-operated.monitoring.svc.cluster.local:9090",
        alias="promServerAddress",
    )
    scaling_operator: Optional[StrictStr] = Field(
        default=None,
        description='ScalingOperator specifies the comparison operator used by KEDA to decide whether to scale the Deployment. Common operators include: - "GreaterThanOrEqual": Scale up when the metric is >= ScalingThreshold. - "LessThanOrEqual": Scale down when the metric is <= ScalingThreshold.  This operator defines the condition under which scaling actions are triggered based on the evaluated metric.  Example:   "GreaterThanOrEqual"',
        alias="scalingOperator",
    )
    scaling_threshold: Optional[StrictStr] = Field(
        default=None,
        description='ScalingThreshold sets the numerical threshold against which the result of the Prometheus query will be compared. Depending on the ScalingOperator, this threshold determines when to scale the number of replicas up or down.  Example:   "10" - The Autoscaler will compare the metric value to 10.',
        alias="scalingThreshold",
    )
    __properties: ClassVar[List[str]] = [
        "customPromQuery",
        "enableKeda",
        "promServerAddress",
        "scalingOperator",
        "scalingThreshold",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of V1beta1KedaConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of V1beta1KedaConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "customPromQuery": obj.get("customPromQuery"),
                "enableKeda": obj.get("enableKeda"),
                "promServerAddress": obj.get("promServerAddress"),
                "scalingOperator": obj.get("scalingOperator"),
                "scalingThreshold": obj.get("scalingThreshold"),
            }
        )
        return _obj
