apiVersion: ome.io/v1beta1
kind: ClusterServingRuntime
metadata:
  name: cohere-command-r-v1
spec:
  disabled: false
  annotations:
    prometheus.io/port: '8081'
    prometheus.io/scrape: 'true'
    ome.io/disable-blocklist: 'true'
    ome.io/inject-model-init: 'true'
  labels:
    logging-forward: enabled
  tolerations:
    - key: "nvidia.com/gpu"
      operator: "Exists"
      effect: "NoSchedule"
  supportedModelFormats:
    - autoSelect: true
      modelArchitecture: CohereForCausalLM
      version: "1.0-16k"
      modelFormat:
        name: tensorrtllm
        version: 'v0.9.0'
      modelFramework:
        name: tensorrtllm
        version: 'v0.9.0'
  modelSizeRange:
    min: 13B
    max: 20B
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: node.kubernetes.io/instance-type
                operator: In
                values:
                  - BM.GPU.H100.8
                  - BM.GPU.A100-v2.8
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          preference:
            matchExpressions:
              - key: node.kubernetes.io/instance-type
                operator: In
                values:
                  - BM.GPU.A100-v2.8
        - weight: 1
          preference:
            matchExpressions:
              - key: node.kubernetes.io/instance-type
                operator: In
                values:
                  - BM.GPU.H100.8
  protocolVersions:
    - cohere
  volumes:
    - name: shared-memory
      emptyDir:
        medium: Memory
        sizeLimit: 1Gi
    - name: model-empty-dir
      emptyDir:
        medium: Memory
  containers:
    - name: ome-container
      image: us-chicago-1.ocir.io/idqj093njucb/cohere-oci-all:v0.5.9
      ports:
        - containerPort: 8080
          name: http1
      resources:
        limits:
          nvidia.com/gpu: "4"
        requests:
          cpu: 16
          memory: 120Gi
          nvidia.com/gpu: "4"
      volumeMounts:
        - mountPath: /dev/shm
          name: shared-memory
        - mountPath: /opt/ml/model
          name: model-empty-dir
      readinessProbe:
        httpGet:
          path: /ping
          port: 8080
        failureThreshold: 5
        successThreshold: 1
        periodSeconds: 10
        timeoutSeconds: 20
      livenessProbe:
        httpGet:
          path: /ping
          port: 8080
        failureThreshold: 5
        successThreshold: 1
        periodSeconds: 10
        timeoutSeconds: 20
      startupProbe:
        httpGet:
          path: /ping
          port: 8080
        failureThreshold: 100
        successThreshold: 1
        periodSeconds: 3
        initialDelaySeconds: 60
        timeoutSeconds: 5
