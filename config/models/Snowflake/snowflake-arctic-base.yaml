apiVersion: ome.io/v1beta1
kind: ClusterBaseModel
metadata:
  name: snowflake-arctic-base
spec:
  modelCapabilities:
    - TEXT_TO_TEXT
  vendor: snowflake
  disabled: false
  version: "1.0.0"
  displayName: snowflake.arctic-base
  storage:
    storageUri: hf://Snowflake/snowflake-arctic-base
    path: /raid/models/snowflake/snowflake-arctic-base
