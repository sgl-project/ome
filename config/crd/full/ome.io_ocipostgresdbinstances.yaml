---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ocipostgresdbinstances.ome.io
spec:
  group: ome.io
  names:
    kind: OciPostgresDBInstance
    listKind: OciPostgresDBInstanceList
    plural: ocipostgresdbinstances
    singular: ocipostgresdbinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.lifecycleState
      name: Ready
      type: string
    - jsonPath: .status.lifecycleState
      name: Lifecycle_State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              adminSecretName:
                type: string
              adminSecretNamespace:
                type: string
              authType:
                default: InstancePrincipal
                enum:
                - InstancePrincipal
                - UserPrincipal
                type: string
              dbClusterId:
                type: string
            required:
            - adminSecretName
            - adminSecretNamespace
            - dbClusterId
            type: object
          status:
            properties:
              appUserSecretName:
                type: string
              appUserSecretNamespace:
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              databaseName:
                type: string
              endpoint:
                type: string
              isReady:
                type: boolean
              lifecycleState:
                enum:
                - READY
                - CREATING
                - DELETING
                - DELETED
                - FAILED
                - UPDATING
                type: string
            required:
            - isReady
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
