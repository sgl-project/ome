apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: mcpservers.ome.io
spec:
  group: ome.io
  names:
    kind: MCPServer
    listKind: MCPServerList
    plural: mcpservers
    singular: mcpserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            type: object
            x-kubernetes-map-type: atomic
            x-kubernetes-preserve-unknown-fields: true
          status:
            type: object
            x-kubernetes-map-type: atomic
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: image is required for Hosted MCP servers
          rule: 'has(self.spec.type) && (self.spec.type == ''Hosted'' ? has(self.spec.image)
            && self.spec.image != '''' : true)'
        - message: url is required for Remote MCP servers
          rule: 'has(self.spec.type) && (self.spec.type == ''Remote'' ? has(self.spec.url)
            && self.spec.url != '''' : true)'
    served: true
    storage: true
    subresources:
      status: {}
