apiVersion: ome.io/v1beta1
kind: InferenceService
metadata:
  name: deepseek-llm-7b-chat
  namespace: deepseek-llm-7b-chat
spec:
  model:
    name: deepseek-llm-7b-chat
  engine:
    minReplicas: 1
    maxReplicas: 1
  # NOTE: runtime.name removed to test auto-select
  router:
    minReplicas: 1
    maxReplicas: 1
