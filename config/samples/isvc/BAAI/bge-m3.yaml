apiVersion: ome.io/v1beta1
kind: InferenceService
metadata:
  name: bge-m3
  namespace: bge-m3
spec:
  model:
    name: bge-m3
  engine:
    minReplicas: 1
    maxReplicas: 1
  router:
    minReplicas: 1
    maxReplicas: 1
