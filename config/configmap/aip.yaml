apiVersion: v1
kind: ConfigMap
metadata:
  name: aiplatform-config
  namespace: ome
data:
  aiplatform: |
    {
      "secretConfig": {
        "writeToCommonNamespace": true,
        "namespace": "ome",
        "secretName": "ome-openai-secrets",
      }
    }