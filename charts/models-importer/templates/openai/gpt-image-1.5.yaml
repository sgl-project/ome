{{- if index .Values "gpt-image-1.5" "enabled" -}}
apiVersion: ome.io/v1beta1
kind: ClusterBaseModel
metadata:
  name: gpt-image-1.5
  labels:
    genai-managed-base-model-v1beta1: "true"
  annotations:
    models.ome.io/runtime: ""
    models.ome.io/category: "LARGE"
    models.ome.io/experimental: "{{ index .Values "gpt-image-1.5" "isExperimental" }}"
    models.ome.io/internal: "{{ index .Values "gpt-image-1.5" "isInternal" }}"
    models.ome.io/lifecycle-phase: "{{ index .Values "gpt-image-1.5" "lifecyclePhase" }}"
spec:
  compartmentID: {{ .Values.compartmentID }}
  disabled: false
  displayName: openai.gpt-image-1.5
  storage:
    storageUri: "vendor://openai/models/{{ index .Values "gpt-image-1.5" "osPrefixOverride" }}"
  vendor: {{ index .Values "gpt-image-1.5" "vendor" }}
  version: "1.0.0"
  modelFormat:
    name: safetensors
    version: "1.0.0"
  modelFramework:
    name: transformer
    version: "1"
  modelArchitecture: OpenAIForCausalLM
  modelParameterSize: 1.8T
  modelCapabilities:
    - "TEXT_TO_IMAGE"
  maxTokens: 128000
  additionalMetadata:
    image-text-to-text: "{{ index .Values "gpt-image-1.5" "enableImageInput" }}"
{{- end }}
