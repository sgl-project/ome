{{- if index .Values "o1" "enabled" -}}
apiVersion: ome.io/v1beta1
kind: ClusterBaseModel
metadata:
  name: o1
  labels:
    genai-managed-base-model-v1beta1: "true"
  annotations:
    models.ome.io/runtime: ""
    models.ome.io/category: "SMALL"
    models.ome.io/experimental: "{{ index .Values "o1" "isExperimental" }}"
    models.ome.io/internal: "{{ index .Values "o1" "isInternal" }}"
    models.ome.io/lifecycle-phase: "{{ index .Values "o1" "lifecyclePhase" }}"
spec:
  compartmentID: {{ .Values.compartmentID }}
  disabled: false
  displayName: openai.o1
  storage:
    storageUri: "vendor://openai/models/{{ index .Values "o1" "osPrefixOverride" }}"
  vendor: {{ index .Values "o1" "vendor" }}
  version: "1.0.0"
  modelFormat:
    name: safetensors
    version: "1"
  modelFramework:
    name: transformer
    version: "1"
  modelArchitecture: OpenAIForCausalLM
  modelParameterSize: 200B
  modelCapabilities:
    - "CHAT"
  maxTokens: 200000

{{- end }}