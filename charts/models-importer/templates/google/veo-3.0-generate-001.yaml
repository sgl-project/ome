{{- if index .Values "veo-3-0-generate-001" "enabled" -}}
apiVersion: ome.io/v1beta1
kind: ClusterBaseModel
metadata:
  name: veo-3-0-generate-001
  labels:
    genai-managed-base-model-v1beta1: "true"
  annotations:
    models.ome.io/runtime: ""
    models.ome.io/category: "SMALL"
    models.ome.io/experimental: "{{ index .Values "veo-3-0-generate-001" "isExperimental" }}"
    models.ome.io/internal: "{{ index .Values "veo-3-0-generate-001" "isInternal" }}"
    models.ome.io/lifecycle-phase: "{{ index .Values "veo-3-0-generate-001" "lifecyclePhase" }}"
spec:
  compartmentID: {{ .Values.compartmentID }}
  disabled: false
  displayName: google.veo-3-0-generate-001
  storage:
    storageUri: "vendor://google/models/{{ index .Values "veo-3-0-generate-001" "osPrefixOverride" }}"
  vendor: {{ index .Values "veo-3-0-generate-001" "vendor" }}
  version: "1.0.0"
  modelFormat:
    name: safetensors
    version: "1.0.0"
  modelFramework:
    name: transformer
    version: "1"
  modelArchitecture: GeminiForCausalLM
  modelParameterSize: 1.8T
  modelCapabilities:
    - "TEXT_TO_VIDEO"
    - "IMAGE_TEXT_TO_VIDEO"
  maxTokens: 1048576
{{- end }}