{{- if index .Values "grok-2-image" "enabled" -}}
apiVersion: ome.io/v1beta1
kind: ClusterBaseModel
metadata:
  name: grok-2-image
  labels:
    genai-managed-base-model-v1beta1: "true"
  annotations:
    models.ome.io/runtime: ""
    models.ome.io/category: "SMALL"
    models.ome.io/experimental: "{{ index .Values "grok-2-image" "isExperimental" }}"
    models.ome.io/internal: "{{ index .Values "grok-2-image" "isInternal" }}"
    models.ome.io/lifecycle-phase: "{{ index .Values "grok-2-image" "lifecyclePhase" }}"
spec:
  compartmentID: {{ .Values.compartmentID }}
  disabled: false
  displayName: xai.grok-2-image
  storage:
    storageUri: "vendor://xai/models/{{ index .Values "grok-2-image" "osPrefixOverride" }}"
  vendor: {{ index .Values "grok-2-image" "vendor" }}
  version: "1.0.0"
  modelFormat:
    name: safetensors
    version: "1.0.0"
  modelFramework:
    name: transformer
    version: "1"
  modelArchitecture: xAiForCausalLM
  modelParameterSize: 300B
  modelCapabilities:
    - "TEXT_TO_IMAGE"
  maxTokens: 256000
  {{- end }}