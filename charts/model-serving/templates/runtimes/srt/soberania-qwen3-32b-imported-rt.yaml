apiVersion: ome.io/v1beta1
kind: ClusterServingRuntime
metadata:
  name: srt-soberania-qwen3-32b-imported
spec:
  acceleratorRequirements:
    acceleratorClasses:
      - nvidia-a100-80gb-1
      - nvidia-a100-80gb-2
      - nvidia-a100-80gb-4
      - nvidia-a100-80gb-8
      - nvidia-h100-1
      - nvidia-h100-2
      - nvidia-h100-4
      - nvidia-h100-8
      - nvidia-h200-1
      - nvidia-h200-2
      - nvidia-h200-4
      - nvidia-h200-8
  disabled: false
  routerConfig:
    annotations:
      prometheus.io/path: /metrics
      prometheus.io/port: '29000'
      prometheus.io/scrape: 'true'
    labels:
      logging-forward: enabled
    tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
    affinity:
      podAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
                - key: component
                  operator: In
                  values:
                    - engine
            matchLabelKeys:
              - ome.io/inferenceservice
            topologyKey: kubernetes.io/hostname
    volumes:
      - name: customer-model-store
        hostPath:
          path: /mnt/data/models/customer-model-store
          type: Directory
    runner:
      name: router
      image: {{ .Values.soberania_qwen3_32b_imported.router.image.repository }}:{{ .Values.soberania_qwen3_32b_imported.router.image.tag }}
      ports:
        - containerPort: 8080
          name: http
      resources:
        requests:
          cpu: "1"
          memory: 2Gi
        limits:
          cpu: "1"
          memory: 2Gi
      args:
        - --host
        - 0.0.0.0
        - --port
        - "8080"
        - --model-path
        - /mnt/data/models/customer-model-store
        - --service-discovery
        - --service-discovery-namespace
        - $(NAMESPACE)
        - --service-discovery-port
        - "{{ .Values.sglang.port }}"
        - --selector
        - component=engine ome.io/inferenceservice=$(INFERENCESERVICE_NAME)
        - --tool-call-parser
        - qwen
        - --reasoning-parser
        - qwen3
        - --enable-igw
        - --request-id-headers
        - opc-request-id
        - --log-json
        - --disable-retries
        - --disable-circuit-breaker
      env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: INFERENCESERVICE_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['ome.io/inferenceservice']
        - name: BASE_MODEL_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['ome.io/base-model-name']
      volumeMounts:
        - name: customer-model-store
          mountPath: /mnt/data/models/customer-model-store
          subPathExpr: $(BASE_MODEL_NAME)
          readOnly: true
      readinessProbe:
        httpGet:
          path: /readiness
          port: 8080
        failureThreshold: 5
        periodSeconds: 30
        timeoutSeconds: 10
      livenessProbe:
        httpGet:
          path: /liveness
          port: 8080
        failureThreshold: 5
        periodSeconds: 30
        timeoutSeconds: 10
      startupProbe:
        httpGet:
          path: /readiness
          port: 8080
        failureThreshold: 10
        periodSeconds: 20
        timeoutSeconds: 10
  {{- if or .Values.commonAnnotations .Values.soberania_qwen3_32b_imported.annotations }}
  annotations:
    {{- if .Values.commonAnnotations }}
    {{ toYaml .Values.commonAnnotations | nindent 4 }}
    {{- end }}
    {{- if .Values.soberania_qwen3_32b_imported.annotations }}
    {{ toYaml .Values.soberania_qwen3_32b_imported.annotations | nindent 4 }}
    {{- end }}
  {{- end }}
  {{- if or .Values.commonLabels .Values.soberania_qwen3_32b_imported.labels }}
  labels:
    {{- if .Values.commonLabels }}
    {{ toYaml .Values.commonLabels | nindent 4 }}
    {{- end }}
    {{- if .Values.soberania_qwen3_32b_imported.labels }}
    {{ toYaml .Values.soberania_qwen3_32b_imported.labels | nindent 4 }}
    {{- end }}
  {{- end }}
  {{- if or .Values.commonTolerations .Values.soberania_qwen3_32b_imported.tolerations }}
  tolerations:
    {{- if .Values.commonTolerations }}
    {{ toYaml .Values.commonTolerations | nindent 4 }}
    {{- end }}
    {{- if .Values.soberania_qwen3_32b_imported.tolerations }}
    {{ toYaml .Values.soberania_qwen3_32b_imported.tolerations | nindent 4 }}
    {{- end }}
  {{- end }}
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
    {{- if or (eq .Values.oci.realm "oc5") (eq .Values.oci.realm "oc6") }}
    - name: etc-pki
      hostPath:
        path: /etc/pki
        type: Directory
    {{- end }}
  {{ with .Values.soberania_qwen3_32b_imported }}
  affinity: {{ .affinity | toYaml | nindent 4 }}
  {{ end }}
  supportedModelFormats:
    - modelFramework:
        name: transformers
        version: "4.56.2"
      modelFormat:
        name: safetensors
        version: "1.0.0"
      autoSelect: true
      modelArchitecture: Qwen3ForCausalLM
      version: "1.0.0"
      priority: 1
      acceleratorConfig:
        nvidia-a100-80gb-1:
          tensorParallelismOverride:
            tensorParallelSize: 1
        nvidia-a100-80gb-2:
          tensorParallelismOverride:
            tensorParallelSize: 2
        nvidia-a100-80gb-4:
          tensorParallelismOverride:
            tensorParallelSize: 4
        nvidia-a100-80gb-8:
          tensorParallelismOverride:
            tensorParallelSize: 8
        nvidia-h100-1:
          tensorParallelismOverride:
            tensorParallelSize: 1
        nvidia-h100-2:
          tensorParallelismOverride:
            tensorParallelSize: 2
        nvidia-h100-4:
          tensorParallelismOverride:
            tensorParallelSize: 4
        nvidia-h100-8:
          tensorParallelismOverride:
            tensorParallelSize: 8
        nvidia-h200-1:
          tensorParallelismOverride:
            tensorParallelSize: 1
        nvidia-h200-2:
          tensorParallelismOverride:
            tensorParallelSize: 2
        nvidia-h200-4:
          tensorParallelismOverride:
            tensorParallelSize: 4
        nvidia-h200-8:
          tensorParallelismOverride:
            tensorParallelSize: 8
  modelSizeRange:
    min: 28.8B
    max: 35.2B
  protocolVersions:
    - openAI
  schedulerName: {{ .Values.commonSchedulerName }}
  containers:
    - name: ome-container
      image: {{ .Values.soberania_qwen3_32b_imported.image.repository | default .Values.sglang.commonImage.repository }}:{{ .Values.soberania_qwen3_32b_imported.image.tag | default .Values.sglang.commonImage.tag }}
      ports:
        - containerPort: {{ .Values.sglang.port }}
          name: grpc1
          protocol: TCP
      command:
        - python3
        - -m
        - sglang.launch_server
      args:
        - --grpc-mode
        - --served-model-name
        - vllm-model
        - --host
        - 0.0.0.0
        - --port
        - "{{ .Values.sglang.port }}"
        - --model-path
        - $(MODEL_PATH)
        - --tp-size
        - "2"
        - --mem-fraction-static
        - "0.9"
        - --context-length
        - "32768"
        - --log-requests
        - --log-requests-level=1
        - --enable-metrics
      volumeMounts:
        - mountPath: /dev/shm
          name: dshm
      resources:
        requests:
          cpu: 28
          memory: 160Gi
          nvidia.com/gpu: 2
        limits:
          cpu: 28
          memory: 160Gi
          nvidia.com/gpu: 2
      readinessProbe:
        grpc:
          port: 8080
        failureThreshold: 3
        successThreshold: 1
        periodSeconds: 60
        timeoutSeconds: 200
      livenessProbe:
        grpc:
          port: 8080
        failureThreshold: 5
        successThreshold: 1
        periodSeconds: 60
        timeoutSeconds: 60
      startupProbe:
        grpc:
          port: 8080
        failureThreshold: 150
        successThreshold: 1
        periodSeconds: 6
        initialDelaySeconds: 60
        timeoutSeconds: 30
  engineConfig:
    schedulerName: {{ .Values.commonSchedulerName }}
    {{- if or .Values.commonAnnotations .Values.soberania_qwen3_32b_imported.annotations }}
    annotations:
      {{- if .Values.commonAnnotations }}
      {{ toYaml .Values.commonAnnotations | nindent 6 }}
      {{- end }}
      {{- if .Values.soberania_qwen3_32b_imported.annotations }}
      {{ toYaml .Values.soberania_qwen3_32b_imported.annotations | nindent 6 }}
      {{- end }}
    {{- end }}
    {{- if or .Values.commonLabels .Values.soberania_qwen3_32b_imported.labels }}
    labels:
      {{- if .Values.commonLabels }}
      {{ toYaml .Values.commonLabels | nindent 6 }}
      {{- end }}
      {{- if .Values.soberania_qwen3_32b_imported.labels }}
      {{ toYaml .Values.soberania_qwen3_32b_imported.labels | nindent 6 }}
      {{- end }}
    {{- end }}
    {{- if or .Values.commonTolerations .Values.soberania_qwen3_32b_imported.tolerations }}
    tolerations:
      {{- if .Values.commonTolerations }}
      {{ toYaml .Values.commonTolerations | nindent 6 }}
      {{- end }}
      {{- if .Values.soberania_qwen3_32b_imported.tolerations }}
      {{ toYaml .Values.soberania_qwen3_32b_imported.tolerations | nindent 6 }}
      {{- end }}
    {{- end }}
    volumes:
      - name: dshm
        emptyDir:
          medium: Memory
      {{- if or (eq .Values.oci.realm "oc5") (eq .Values.oci.realm "oc6") }}
      - name: etc-pki
        hostPath:
          path: /etc/pki
          type: Directory
      {{- end }}
    {{ with .Values.soberania_qwen3_32b_imported }}
    affinity: {{ .affinity | toYaml | nindent 6 }}
    {{ end }}
    runner:
      name: ome-container
      image: {{ .Values.soberania_qwen3_32b_imported.image.repository | default .Values.sglang.commonImage.repository }}:{{ .Values.soberania_qwen3_32b_imported.image.tag | default .Values.sglang.commonImage.tag }}
      ports:
        - containerPort: {{ .Values.sglang.port }}
          name: grpc1
          protocol: TCP
      command:
        - python3
        - -m
        - sglang.launch_server
      args:
        - --grpc-mode
        - --served-model-name
        - vllm-model
        - --host
        - 0.0.0.0
        - --port
        - "{{ .Values.sglang.port }}"
        - --model-path
        - $(MODEL_PATH)
        - --tp-size
        - "2"
        - --mem-fraction-static
        - "0.9"
        - --context-length
        - "32768"
        - --log-requests
        - --log-requests-level=1
        - --enable-metrics
      volumeMounts:
        - mountPath: /dev/shm
          name: dshm
      resources:
        requests:
          cpu: 28
          memory: 160Gi
          nvidia.com/gpu: 2
        limits:
          cpu: 28
          memory: 160Gi
          nvidia.com/gpu: 2
      readinessProbe:
        grpc:
          port: 8080
        failureThreshold: 3
        successThreshold: 1
        periodSeconds: 60
        timeoutSeconds: 200
      livenessProbe:
        grpc:
          port: 8080
        failureThreshold: 5
        successThreshold: 1
        periodSeconds: 60
        timeoutSeconds: 60
      startupProbe:
        grpc:
          port: 8080
        failureThreshold: 150
        successThreshold: 1
        periodSeconds: 6
        initialDelaySeconds: 60
        timeoutSeconds: 30
