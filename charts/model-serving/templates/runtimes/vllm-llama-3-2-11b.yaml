apiVersion: ome.io/v1beta1
kind: ClusterServingRuntime
metadata:
  name: vllm-llama-3-2-11b
spec:
  {{with .Values.commonAnnotations}}
  annotations: {{ toYaml . | nindent 6 }}
  {{end}}
  {{with .Values.commonLabels}}
  labels: {{ toYaml . | nindent 6 }}
  {{end}}
  {{with .Values.commonTolerations}}
  tolerations: {{ toYaml . | nindent 6 }}
  {{end}}
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: node.kubernetes.io/instance-type
                operator: In
                values:
                  - BM.GPU.H100.8
  supportedModelFormats:
    - name: safetensors
      modelType: transformer
      modelArchitecture: MllamaForConditionalGeneration
      version: "1"
      autoSelect: true
      priority: 1
  protocolVersions:
    - openAI
  containers:
    - name: ome-container
      image: {{ .Values.vllm.commonImage.repository }}:{{ .Values.vllm.llama3_2_image.tag }}
      ports:
        - containerPort: 8080
          name: http
          protocol: TCP
      command:
        - /bin/bash
        - '-lc'
        - --
      args:
        - |
          python3 -m vllm.entrypoints.openai.api_server \
          --port={{.Values.vllm.port}} \
          --model="$MODEL_PATH" \
          --disable-log-requests \
          --served-model-name={{.Values.vllm.serveModelName}} \
          --tensor-parallel-size=1 \
          --max-num-seqs=32 \
          --enforce-eager \
          --limit-mm-per-prompt="image=1" \
          --max-model-len=128000 \
          --gpu-memory-utilization=0.95
      volumeMounts:
        - mountPath: /dev/shm
          name: dshm
      resources:
        requests:
          cpu: 10
          memory: 160Gi
          nvidia.com/gpu: 1
        limits:
          cpu: 10
          memory: 160Gi
          nvidia.com/gpu: 1

      readinessProbe:
        httpGet:
          path: /health
          port: 8080
        failureThreshold: 3
        successThreshold: 1
        periodSeconds: 60
        timeoutSeconds: 200

      livenessProbe:
        httpGet:
          path: /health
          port: 8080
        failureThreshold: 5
        successThreshold: 1
        periodSeconds: 60
        timeoutSeconds: 60

      startupProbe:
        httpGet:
          path: /health
          port: 8080
        failureThreshold: 150
        successThreshold: 1
        periodSeconds: 6
        initialDelaySeconds: 60
        timeoutSeconds: 30