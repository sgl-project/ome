# proxy-models-importer

A Helm chart for importing proxy model ClusterBaseModel CRs (models forwarded to external vendor APIs).

This chart is deployed by the `proxy-model-pipeline` Shepherd flock, separate from the `model-pipeline-devops` flock that handles self-hosted models.

