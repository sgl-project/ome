{{- if index .Values "claude-default" "enabled" -}}
apiVersion: ome.io/v1beta1
kind: ClusterBaseModel
metadata:
  name: claude-default
  labels:
    {{ include "proxy-models-importer.genaiModelLabels" (dict "root" $ "model" "claude-default") | nindent 4 }}
  annotations:
    {{ include "proxy-models-importer.genaiModelAnnotations" (dict "root" $ "model" "claude-default") | nindent 4 }}
spec:
  compartmentID: {{ .Values.compartmentID }}
  disabled: false
  displayName: anthropic.claude-default
  storage:
    storageUri: "vendor://anthropic/models/{{ index .Values "claude-default" "osPrefixOverride" }}"
  vendor: {{ index .Values "claude-default" "vendor" }}
  version: "1.0.0"
  modelFormat:
    name: safetensors
    version: "1.0.0"
  modelFramework:
    name: transformer
    version: "1"
  modelArchitecture: OpenAIForCausalLM
  modelParameterSize: 1.8T
  modelCapabilities:
    - "CHAT"
  apiCapabilities:
    - "API_CAPABILITIES_PLACEHODER"
  maxTokens: 64000
  additionalMetadata:
    image-text-to-text: "{{ index .Values "claude-default" "enableImageInput" }}"
{{- end }}
