apiVersion: ome.io/v1beta1
kind: ClusterServingRuntime
metadata:
  name: vllm-serving-runtime-medium
spec:
  annotations:
    prometheus.ome.io/port: '8080'
    prometheus.ome.io/path: "/metrics"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: node.kubernetes.io/instance-type
                operator: In
                values:
                  - BM.GPU.B4.8
                  - BM.GPU4.8
                  - BM.GPU.A100-v2.8
                  - BM.GPU.H100.8
  supportedModelFormats:
    - name: safetensors
      modelType: transformer
      modelArchitecture: LlamaForCausalLM
      version: "1"
      autoSelect: true
      priority: 1
  protocolVersions:
    - openAI
  containers:
    - name: ome-container
      image: {{ .Values.ome.servingruntime.vllmMedium.image }}:{{ .Values.ome.servingruntime.vllmMedium.tag }}
      args:
        - --served-model-name=vllm-model
        - --disable-log-stats
        - --enable-chunked-prefill
        - --gpu-memory-utilization=0.9
        - --port=8080
        - --tensor-parallel-size=4
        - --disable-log-requests
        - --model="$MODEL_PATH"
      volumeMounts:
        - mountPath: /dev/shm
          name: dshm
      resources:
        requests:
          cpu: 70
          memory: 216Gi
          nvidia.com/gpu: 4
        limits:
          cpu: 70
          memory: 216Gi
          nvidia.com/gpu: 4

      readinessProbe:
        httpGet:
          path: /health
          port: 8080
        failureThreshold: 3
        successThreshold: 1
        periodSeconds: 60
        timeoutSeconds: 200

      livenessProbe:
        httpGet:
          path: /health
          port: 8080
        failureThreshold: 5
        successThreshold: 1
        periodSeconds: 60
        timeoutSeconds: 60

      startupProbe:
        httpGet:
          path: /health
          port: 8080
        failureThreshold: 150
        successThreshold: 1
        periodSeconds: 6
        initialDelaySeconds: 60
        timeoutSeconds: 30